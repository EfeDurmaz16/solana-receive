/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from '@solana/kit/program-client-core';
import { TOKEN2022_RECEIVE_PROGRAM_ADDRESS } from '../programs/index.ts';

export const ENSURE_GUARD_DISCRIMINATOR = 3;

export function getEnsureGuardDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(ENSURE_GUARD_DISCRIMINATOR);
}

export type EnsureGuardInstruction<
  TProgram extends string = typeof TOKEN2022_RECEIVE_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountReceiver extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountGuardToken extends string | AccountMeta<string> = string,
  TAccountGuardState extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountReceiver extends string ? ReadonlyAccount<TAccountReceiver> : TAccountReceiver,
      TAccountMint extends string ? ReadonlyAccount<TAccountMint> : TAccountMint,
      TAccountGuardToken extends string ? WritableAccount<TAccountGuardToken> : TAccountGuardToken,
      TAccountGuardState extends string ? WritableAccount<TAccountGuardState> : TAccountGuardState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type EnsureGuardInstructionData = { discriminator: number };

export type EnsureGuardInstructionDataArgs = {};

export function getEnsureGuardInstructionDataEncoder(): FixedSizeEncoder<EnsureGuardInstructionDataArgs> {
  return transformEncoder(getStructEncoder([['discriminator', getU8Encoder()]]), value => ({
    ...value,
    discriminator: ENSURE_GUARD_DISCRIMINATOR,
  }));
}

export function getEnsureGuardInstructionDataDecoder(): FixedSizeDecoder<EnsureGuardInstructionData> {
  return getStructDecoder([['discriminator', getU8Decoder()]]);
}

export function getEnsureGuardInstructionDataCodec(): FixedSizeCodec<
  EnsureGuardInstructionDataArgs,
  EnsureGuardInstructionData
> {
  return combineCodec(
    getEnsureGuardInstructionDataEncoder(),
    getEnsureGuardInstructionDataDecoder(),
  );
}

export type EnsureGuardInput<
  TAccountPayer extends string = string,
  TAccountReceiver extends string = string,
  TAccountMint extends string = string,
  TAccountGuardToken extends string = string,
  TAccountGuardState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  receiver: Address<TAccountReceiver>;
  mint: Address<TAccountMint>;
  guardToken: Address<TAccountGuardToken>;
  guardState: Address<TAccountGuardState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getEnsureGuardInstruction<
  TAccountPayer extends string,
  TAccountReceiver extends string,
  TAccountMint extends string,
  TAccountGuardToken extends string,
  TAccountGuardState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TOKEN2022_RECEIVE_PROGRAM_ADDRESS,
>(
  input: EnsureGuardInput<
    TAccountPayer,
    TAccountReceiver,
    TAccountMint,
    TAccountGuardToken,
    TAccountGuardState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): EnsureGuardInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountReceiver,
  TAccountMint,
  TAccountGuardToken,
  TAccountGuardState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? TOKEN2022_RECEIVE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    receiver: { value: input.receiver ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    guardToken: { value: input.guardToken ?? null, isWritable: true },
    guardState: { value: input.guardState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('receiver', accounts.receiver),
      getAccountMeta('mint', accounts.mint),
      getAccountMeta('guardToken', accounts.guardToken),
      getAccountMeta('guardState', accounts.guardState),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getEnsureGuardInstructionDataEncoder().encode({}),
    programAddress,
  } as EnsureGuardInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountReceiver,
    TAccountMint,
    TAccountGuardToken,
    TAccountGuardState,
    TAccountSystemProgram
  >);
}

export type ParsedEnsureGuardInstruction<
  TProgram extends string = typeof TOKEN2022_RECEIVE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    receiver: TAccountMetas[1];
    mint: TAccountMetas[2];
    guardToken: TAccountMetas[3];
    guardState: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: EnsureGuardInstructionData;
};

export function parseEnsureGuardInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedEnsureGuardInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 6,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      receiver: getNextAccount(),
      mint: getNextAccount(),
      guardToken: getNextAccount(),
      guardState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getEnsureGuardInstructionDataDecoder().decode(instruction.data),
  };
}
